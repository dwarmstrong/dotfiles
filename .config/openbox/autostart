# ~/.config/openbox/autostart
#
# Source: https://github.com/vonbrownie/dotfiles/blob/master/.config/openbox/autostart

# These things are run when an Openbox X Session is started.

# Set background color (`hsetroot` plays nice with compton) and image
hsetroot -solid "#000000"
(sleep 1; ~/.fehbg) &

# Detect if secondary display attached and make it primary
~/bin/dldsply -r &

# Compositor
compton -b -c &

# Detect keyboard and configure xbindkeys + xmodmap
#(sleep 5; ~/bin/keyboardconf) &

# Power management
xfce4-power-manager &

# Notifications
dunst -config ~/.config/dunst/dunstrc &

# Panel - taskbar, system tray, battery, clock
tint2 &

# Clipboard
clipit &

# Volume control
(sleep 2; volumeicon) &

# Network manager (launched by /etc/xdg/autostart/nm-applet.desktop)
#nm-applet &

# Launch terminal
urxvt &
